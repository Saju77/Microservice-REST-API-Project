server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Saju77/ctrends_mircroservice_config_server
          clone-on-start: true